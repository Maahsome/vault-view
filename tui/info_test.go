package tui

import (
	"testing"
)

// TestNewInfo - testing tui.newInfo Function
func TestNewInfo(t *testing.T) {

	// os.Setenv("VAULT_ADDR", "https://127.0.0.1:8200")
	// os.Setenv("VAULT_TOKEN", "1234567890")

	// vaultCli := vault.NewVaultMock()
	// tv := newInfo("v0.0.1")

	// const info = " Vault Address:     https://127.0.0.1:8200\n Vault Version:     1.2.3\n vault-view:        v0.0.1"
	// assert.Equal(t, tv.GetText(true), info)
}
